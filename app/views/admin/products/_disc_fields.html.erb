<div class="nested-fields">

  <div class="form-group">
    <label>ディスクNo.</label>
    <%= f.select :disc_num, [1,2,3], class: "form-control" %>
  </div>

  <div>
    <%= f.fields_for :songs do |d| %>
      <%= render "song_fields", f: d %>
    <% end %>
  </div>

  <div>
    <%= link_to_add_association "曲を追加", f, :songs %>
  </div>

</div>
