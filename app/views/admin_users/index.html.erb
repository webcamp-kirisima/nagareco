<table class="table table-striped">
  <thead>
    <tr>
      <th> id</th><th> 苗字</th> <th> 名前　</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
    <tr>
  </div>
      <td><%= user.id %></td>
      <td><%= user.last_name %></td>
      <td><%= user.first_name %></td>
      <td><%= link_to "詳細", user_path(user.id) %></td>
    </tr>
    <% end %>
  </tbody>
</table>